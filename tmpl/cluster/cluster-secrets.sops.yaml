# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_ADGUARD_USER: ${BOOTSTRAP_ADGUARD_USER}
  SECRET_ADGUARD_PASS_HASH: ${BOOTSTRAP_ADGUARD_PASS_HASH}